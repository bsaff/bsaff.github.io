name: Deploy Astro site to GitHub Pages

on:
  push:
    branches: [master]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout repo
        uses: actions/checkout@v4

      - name: 🧰 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🛠️ Build Astro
        run: npm run build

      - name: 🔍 Pre-download upload-artifact@v3
        uses: actions/upload-artifact@v3
        with:
          name: workaround
          path: README.md

      - name: 📦 Upload site artifact
        uses: actions/upload-pages-artifact@v2
        with:
          path: dist
